{
  "name": "delete-test-01",
  "description": "delete-test-01-description",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@types/aws-lambda": "^8.10.59",
    "@types/node": "^14.0.24",
    "@types/uuid": "^8.0.0",
    "aws-sdk": "^2.437.0",
    "typescript": "^3.9.7",
    "uuid": "^8.2.0"
  },
  "devDependencies": {
    "jest": "^24.7.1"
  },
  "scripts": {
    "test": "cd sam-project && jest",
    "sam:institute": "cd sam/nta-institute && sam local start-api",
    "build": "npm run build:tsc && npm run build:clean && npm run build:transfer",
    "build:tsc": "tsc",
    "build:clean": "scripts/build.clean.sh",
    "build:transfer": "scripts/build.transfer.sh",
    "build:sam": "cd sam && sam build && cd ..",
    "deploy": "cd sam && sam deploy && cd ..",
    "release": "npm run build && npm run deploy"
  }
}
